# Set up gcloud with the kubectl auth plugin

- Added the Google Cloud CLI to the development recipe alongside the other cloud tooling.
- Included the GKE gcloud auth plugin so `kubectl` can authenticate to GKE clusters using credentials managed by gcloud.
