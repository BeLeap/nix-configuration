# Update README Information

Updated `README.md` to match the current repository layout:

- Replaced stale `config/recipes.nix` and `config/default.nix` references.
- Documented `config/recipe/default/default.nix` as the shared default recipe set.
- Documented `config/recipe-loader.nix` as the recipe loader.
- Removed the stale caveat claiming recipe imports are only one level deep.
