# Use Alt+0 for AeroSpace workspace 10

- Added an AeroSpace main-mode binding that switches to workspace 10 with
  `Alt+0`.
- Kept the generated `Alt+1` through `Alt+9` workspace bindings unchanged.
