# Use llm-agents opencode package

- Set `programs.opencode.package = pkgs.llm-agents.opencode` in `config/recipe/opencode/default.nix` so Home Manager uses the opencode package from the llm-agents overlay.
