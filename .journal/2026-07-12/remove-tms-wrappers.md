# remove tms wrappers

- Removed the `tms` and `tmsf` compatibility wrappers from `beleap-utils` because tmux is no longer part of the workflow.
- Kept `wzs` and `wzsf` unchanged for WezTerm workspace switching.
