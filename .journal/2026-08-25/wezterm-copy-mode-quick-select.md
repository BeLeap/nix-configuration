# WezTerm copy mode quick select shortcut

- Extended WezTerm's default copy mode key table so `Cmd+j` activates Quick Select without replacing the existing copy mode bindings.
- Kept the key table lookup behind WezTerm's GUI availability check so non-GUI configuration evaluation remains supported.
