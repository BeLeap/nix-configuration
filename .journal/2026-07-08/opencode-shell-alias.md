## Opencode Shell Alias

- Added `oc = "opencode"` to the `opencode` Home Manager recipe via `home.shellAliases`.
- This keeps the alias tied to the recipe, so hosts that enable `opencode` receive the alias automatically.
