# Add WezTerm push quick-select pattern

- Added `push-.*` to WezTerm's extra quick-select patterns so push-prefixed text is selectable alongside WezTerm's default patterns.
- Validated the change with `git diff --check` and a targeted configuration assertion.
