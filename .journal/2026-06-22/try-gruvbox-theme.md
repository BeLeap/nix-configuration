# Try Gruvbox theme

- Changed Alacritty from `nord` to `gruvbox_dark`.
- Changed Helix from `nord` to `gruvbox`.
- Replaced the tmux Nord plugin with `pkgs.tmuxPlugins.gruvbox` and set `@tmux-gruvbox` to `dark`.
- Overrode the Gruvbox tmux status formats to avoid Powerline glyphs so patched fonts are not required.
- Generated a Gruvbox-inspired wallpaper at `files/gruvbox-wallpaper.jpg`.
- Updated the macOS wallpaper launch agent to use the Gruvbox wallpaper.
