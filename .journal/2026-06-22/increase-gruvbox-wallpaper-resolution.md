# Increase Gruvbox wallpaper resolution

- Replaced `files/gruvbox-wallpaper.jpg` with a `3840x2160` version.
- Regenerated the wallpaper with a cleaner pixel-art prompt, then upscaled it with point filtering so the pixel-inspired edges stay crisp instead of smeared.
- Kept the same file path so the macOS wallpaper configuration does not need to change.
